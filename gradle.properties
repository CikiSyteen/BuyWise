# Project-wide Gradle settings.

# 末尾两个 trustStore 参数：本机处于 TLS 拦截代理网络，让 Gradle daemon 使用 Windows 根证书库
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -Djavax.net.ssl.trustStoreType=Windows-ROOT -Djavax.net.ssl.trustStore=NUL
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true
# Non-transitive R classes: only reference R classes of your own module
android.nonTransitiveRClass=true

kotlin.code.style=official
